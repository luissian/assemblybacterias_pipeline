/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 *   nextflow run nf-core/assemblybacterias -profile test,<docker/singularity>
 */

params {
  config_profile_name = 'Nanopore local testing'
  config_profile_description = 'Profile containing the paths used in the HPC in the ISCIII, intended for direct usage inside this institution'

  kmerfinder_bacteria_database = '20190108_stable_dirs_bacteria/'
  kmerfinder_bacteria_taxonomy = '20190108_stable_dirs_bacteria/bacteria.name'

  // Input data
  // TODO nf-core: Specify the paths to your test data on nf-core/test-datasets
  // TODO nf-core: Give any required params for the test so that command line flags are not needed

  schema_ignore_params = 'genomes,input_paths,input'
  input = "samplesheet.csv"
 
}

process {

    conda = /home/g.gorines/.conda/envs/assembly"

    withName:KMERFINDER {
        singularity {
        enabled = true
        autoMounts = true
        }
    container = 'kmerfinder_v3.0.2.sif'
  }
}   
